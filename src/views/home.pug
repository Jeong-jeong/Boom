//- HTML 생성
doctype html
html(lang="ko")
	head
		meta(charset="UTF-8")
		meta(http-equiv="X-UA-Compatible", content="IE=edge")
		meta(name="viewport", content="width=device-width, initial-scale=1.0")
		link(rel="stylesheet", href="https://unpkg.com/mvp.css")
		title Boom
	body
		header
			h1 Boom 📸
		main
			div.welcome
				form
					input.welcomeInput(placeholder="방문할 방을 입력해주세요" type="text" required)
					input.nicknameInput(placeholder="닉네임을 만들어주세요" type="text" required)
					button 입장하기
				h4 대기중인 방
				ul
			div.room
				h3
				ul
				form
					input(placeholder="메시지를 입력해주세요" type="text" required)
					button 보내기
		script(src="/socket.io/socket.io.js") 
		script(src="/public/js/app.js") 